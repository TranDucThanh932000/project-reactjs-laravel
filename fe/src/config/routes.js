const routes = {
    blog: '/',
    blogDetail: '/:blogId',
    login: '/login',
    register: '/register',
    luckyNumberToday: '/lucky-number-today',
    qrcode: '/qr-code',
};

export default routes;
